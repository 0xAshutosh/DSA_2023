#include <iostream>
using namespace std;

int main() {
    // int n;
    // cin >> n;

    // int ans = 0;

    // if (n < 0) n *= -1;

    // while (n != 0) {
    //     if (n & 1) ans++;
    //     n = n >> 1;
    // }

    // cout << ans << endl; 

    char ch = -66;
    cout << ch << endl;
}