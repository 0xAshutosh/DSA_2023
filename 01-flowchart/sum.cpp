/* write a program to find a sum of two number using flowchart */
#include <iostream>
using namespace std;

int main(void) {
    int a, b;
    cin >> a >> b;
    int sum = a + b;
    cout << sum << endl;

    return 0;
}